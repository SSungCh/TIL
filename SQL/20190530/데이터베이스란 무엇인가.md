# 데이터베이스란 무엇인가?

* 대량의 정보를 컴퓨터가 효율적으로 접근할 수 있도록 가공 및 저장

* 데이터베이스 관리 시스템(DBMS) 을 사용함으로써 다수의 사람이 안전하고 쉽게 다룰 수 있다.

* 관계형 데이터베이스(SQL) 이라는 전용 언어를 사용해서 조작하는 방법을 배운다.

* 관계형 데이터베이스는 관계형(Relational) 데이터베이스 관리 시스템(RDBMS)를 통해 관리한다.



#### SQL

Oracle, MySQL 각각의 SQL이 있음. 표준은 미국



#### 키-밸류형 데이터스토어(KVS)

#### 1. RDBMS

#### 2. DDL

```sql
##### CREATE

CREATE TABLE  T_USER(

​	ID VARCHAR2(10),

​    PWD VARCHAR2(10),

​    NAME VARCHAR2(20)

);

##### DROP

DROP TABLE T_USER;

##### ALTER

ALTER TABLE T_PRODUCT ADD  PRIMARY KEY(ID);

ALTER TABLE T_PRODUCT MODIFY(NAME NULL));

// 속성을 바꿀수도 있다.

ALTER TABLE T_PRODUCT RENAME COLUMN NAME TO UNMAE;

// 이름을 바꿀수도 있다.

ALTER TABLE T_PRODUCT RENAME TO PRODUCT;

// 테이블 이름도 바꿀수 있다.



```

### 3. DML

``` sql

### 테이블 생성 DML
INSERT INTO T_USER (ID,PWD,NAME)
VALUES('ID01','PWD01','이말숙');

ALTER TABLE T_USER ADD PRIMARY KEY(ID);
INSERT INTO T_USER VALUES ('ID01','PW01','이말숙');
INSERT INTO T_USER VALUES ('ID02','PW02','히말숙');
INSERT INTO T_USER VALUES ('ID05','PW05','박말숙');

SELECT * FROM T_USER;
DELETE FROM T_USER;

DELETE FROM T_USER WHERE ID='ID05'// 특정 ID지울떄 사용

INSERT INTO T_USER VALUES ('ID06','PW06',NULL);
INSERT INTO T_USER VALUES ('ID07','PW07',' ');

이름에 널값이 들어가면 안됨 >> 

ALTER TABLE T_USER MODIFY (PWD NOT NULL, NAME NOT NULL)

##### CREATE

CREATE TABLE  T_USER(

​	ID VARCHAR2(10),

​    PWD VARCHAR2(10),

​    NAME VARCHAR2(20)

);

##### DROP

DROP TABLE T_USER;

##### ALTER

ALTER TABLE T_PRODUCT ADD  PRIMARY KEY(ID);

ALTER TABLE T_PRODUCT MODIFY(NAME NULL));

// 속성을 바꿀수도 있다.

ALTER TABLE T_PRODUCT RENAME COLUMN NAME TO UNMAE;

// 이름을 바꿀수도 있다.

ALTER TABLE T_PRODUCT RENAME TO PRODUCT;

// 테이블 이름도 바꿀수 있다.



### 테이블 생성 DML

INSERT INTO T_USER (ID,PWD,NAME)
VALUES('ID01','PWD01','이말숙');

ALTER TABLE T_USER ADD PRIMARY KEY(ID);
INSERT INTO T_USER VALUES ('ID01','PW01','이말숙');
INSERT INTO T_USER VALUES ('ID02','PW02','히말숙');
INSERT INTO T_USER VALUES ('ID05','PW05','박말숙');

SELECT * FROM T_USER;
DELETE FROM T_USER;

DELETE FROM T_USER WHERE ID='ID05'// 특정 ID지울떄 사용

INSERT INTO T_USER VALUES ('ID06','PW06',NULL);
INSERT INTO T_USER VALUES ('ID07','PW07',' ');

이름에 널값이 들어가면 안됨 >> 

ALTER TABLE T_USER MODIFY (PWD NOT NULL, NAME NOT NULL)

##### PRIMARY KEY 가 아닌 것을 중복아니게 할때는?

ALTER TABLE T_USER MODIFY(NAME UNIQUE)

##### 초기 DEFAULT 값 선언

ALTER TABLE T_PRODUCT MODIFY (PRICE DEFAULT 1000);

### UPDATE
UPDATE T_USER SET PWD = '111',NAME='공말숙' WHERE ID='ID03';



```



